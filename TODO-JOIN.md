# Fix Join Event Errors

## Steps:
- [x] Step 1: Edit components/join/join-flow.tsx - Show exact Supabase error.message and handle upload fail
- [ ] Step 2: Run `npx @vercel/blob@latest dev` (Vercel login if prompted)
- [ ] Step 3: Test /join?code=[your-event-code], skip selfie if needed
- [x] Complete

Current progress: Code updated. Run Vercel Blob dev server in new terminal, then test join (skip selfie OK for now).
